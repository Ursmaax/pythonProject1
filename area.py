from operator import length_hint

width = input('enter width here :')
length = input('enter length here : ')
width = int(width)
length = int(length)
print(f"area of the rectangle is {width * length} !")

